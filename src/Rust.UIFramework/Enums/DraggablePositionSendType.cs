namespace Oxide.Ext.UiFramework.Enums;

//TODO: Remove once enum is exposed
public enum DraggablePositionSendType : byte
{
    NormalizedScreen = 0,
    NormalizedParent = 1,
    Relative = 2,
    RelativeAnchor = 3,
}